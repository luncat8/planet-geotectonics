# 0.3 — GPU smoothness and measurement plan

Continues `0.2-plan.md` after Phase H. The CPU engine and the GPU kernel graph
are done and parity-gated. What is left: the GPU engine must *feel* as good as
the CPU one, and every claim in this plan must be backed by a measured number,
not by an average.

Symptoms on hardware (the reason this plan exists), from the owner's rig
(`experiments/logs/0.3-2.txt`, nvidia/ampere, Chrome 151, L5, dt 0.1):

- GPU play shows periodic lags and is not as smooth as CPU play: playing 1
  step/frame the strip reads `108.4 fps · frame 34.4 ms · p95 66.6 ms · max
  77.9 ms · events 4 (114.7 ms)` while the CPU engine at the same settings reads
  `90.3 fps · frame 11.3 ms · p95 11.2 ms · max 16.7 ms` with no event line at
  all.
- The event round trip dominates: 4 cycles inside one 500 ms strip window, each
  ~114.7 ms, against a ~9 ms frame interval. At 1 step/frame an event is due
  every ~93 ms, so the round trip costs more than the time between round trips.
- The perf strip's two averages disagree (`108.4 fps` is 9.2 ms, `frame 34.4
  ms`) because one is E[1/gap] and the other E[gap] over a skewed distribution —
  which is exactly why the strip also carries p95/max.
- Throughput scales weakly with the steps/frame setting: the step-isolation bench
  gives 8.900 ms/step at 1 step and 7.220 ms/step at 5, a 1.23× gap, so the
  per-step fixed cost is ~1.7 ms of a ~7 ms step.

Suspects, in the order the measurements settled them:

1. **The event round trip — confirmed and root-caused (Phase II).** It was not
   the download or `Events.cycle`: measured headless, the cycle is ~2 ms and the
   pack/unpack was ~200 ms at L5, of which the dominant part was a per-element
   `ArrayBuffer` allocation in the f32-bits reinterpret of the edges block, plus
   shipping arrays the frame kernels recompute anyway, plus walking dead column
   rows. See `experiments/logs/0.3-II-roundtrip-cost.txt`.
2. **Per-step fixed cost — real but small (Phase V).** ~1.7 ms/step at L5 on
   this rig, so one encoder per rAF is worth ~20% at 1 step/frame, not the 2.5×
   an earlier rig suggested.
3. **`loserRank`'s O(losers × n) scan — measured small at L5 (Phase VI).** The
   timestamp table on the owner's rig puts it third at 0.29 ms behind `winners`
   0.56 and `diagC` 0.41, i.e. ~4% of a 7 ms step in a quiet frame; it still
   matters in heavy-overlap epochs.
4. **Checkpoint every 20 Myr — same transfer as the event round trip**, so it
   shrinks with it (Phase II); it keeps the full mirror because
   `Checkpoint.save` serializes it.
5. **The K11 diagnostic passes, every frame — new, and the largest remaining
   single item.** On a saturated queue `diagA` + `diagC` are 2.67 ms of the
   6.74 ms top-10 kernel total at L5, and no device code reads their output
   (Phase IV).

Atomics (per-cell cursors, ≤~40-contender plate counters) and branching
(ring ≤6, donors ≤3, loser chains 6 fixed dispatches) were reviewed and are
not the cause; this plan does not touch them.

Order: **instrument and capture a baseline → make the round trip cheap → make
the smoke actually measure on hardware (readback + the D1 cross-engine diff) →
diagnostics off the frame path → N steps in one encoder → `loserRank` → polish
and extend to L6.**

## 0. Conventions

Per `0.2-plan.md` §0 and `AGENTS.md`, plus:

- Distribution stats are first-class: the perf strip keeps its EMA for
  averages, but reports p95/max frame gap over a 1 s window (preallocated ring,
  rebuilt at the same 2 Hz, no allocation in the frame loop) and the event
  round trip split into its three phases, `events N (ms = dl a [wait w] + cyc b +
  up c)`, plus the checkpoint push time. A single total cannot say whether a
  hitch is the queue drain or the JS, and `dl` alone cannot say whether the drain
  or the readback is the cost.
- The strip is one row per part (`Perf.rows`: throughput, frame-gap distribution,
  event round trips, checkpoint, kernel table) and both rigs carry a copy control
  — `index.html`'s strip is itself the control (a click anywhere on the rows
  copies the engine/level/dt/steps/view/seed header plus the rows plus `t` and
  the badge, acknowledged by a class rather than a text swap because the rows are
  the report), `bench.html`'s button copies the result table as tab-separated
  text with the kernel line and the raw `BENCH` lines under it, and
  `webgpu-smoke.html` already had one. A number that has to be retyped from a
  screen is a number that arrives wrong or not at all; every capture in
  `experiments/logs/` since has been a paste.
- The frame loop counts frames the play path actually submitted, not the frames
  it asked for, so `steps/s` and `fps` describe the same frames.
- Every phase ends with the regression gate: `node tests/run-all.js --full` (the short
  default is the iterating set; the histories - `longrun` above all - are the gate, and
  they stay off a sandbox: `run_full_test.py` runs them on a real machine), the GPU
  parity ensemble (`node tests/gpu-parity.js 1000 --ensemble` over the headless
  rig) and a bench run compared against the previous phase's committed baseline.
  A phase that moves world evolution (event timing) must keep the release
  profile bit-identical or re-prove the long-run statistics; the parity ensemble
  is the arbiter.
- The JS side of the GPU engine runs headless: `tests/gpu-stub.js` is a stub
  device with real backing stores, so `tests/gpu-play.js` executes the mirror
  transfer and the play scheduling in `node tests/run-all.js`, and
  `experiments/roundtrip-cost.js` times them. No WGSL runs there — the kernel
  graph stays the browser rigs' job.
- Baselines are committed runs in `experiments/logs/`, named
  `0.3-<phase>-<description>.txt`. Existing: `0.3-1.txt` and `0.3-2.txt` (owner's
  rig, Phase I baseline), `0.3-phaseI-baseline-swiftshader.txt` (sandbox: no
  adapter), `0.3-II-roundtrip-cost.txt` (round trip before/after),
  `0.3-II-ondevice.txt` and `0.3-II-ondevice-2.txt` (owner's rig, Phase II
  verified, then the capture that named the smoke's remaining failures).

## Phase I — Instrumentation and baseline (done)

Goal was: make the suspects visible as numbers, then record the "before".

- **I1. GPU query sets in `GpuSim.frame`.** A timestamp query set resolves
  dispatch-group boundaries into a preallocated buffer, read back one frame late
  and folded into the 2 Hz strip as per-kernel GPU ms. Feature-detected, with a
  wall-clock fallback (`tsOn`). On the owner's rig: `tsCollect 1 ms · winners
  0.56 diagC 0.41 loserRank 0.29 diagA 0.26 ledgerReduceA 0.15`.
- **I2. Perf strip distribution stats.** Frame-gap ring + p95/max, the event
  round trip in three phases, the checkpoint push time.
- **I3. Bench, two modes, both time-boxed.** `bench.html` (standalone; also
  `index.html?bench=1`) and `experiments/gpu-bench.js` over the headless rig:
  step isolation (submit n frames, await `onSubmittedWorkDone`) and smoothness
  (the play path under rAF, median/p95/max gap and hitches > 2× median).
- **Baseline committed:** L5/L6 × 1/5 steps, dt 0.1 (`0.3-2.txt`):

  | level | steps | iso ms/step | smooth med | p95 | max | hitches | Myr/s |
  |---|---|---|---|---|---|---|---|
  | L5 | 1 | 8.900 | 5.6 | 111.1 | 122.3 | 33 | 6.3 |
  | L5 | 5 | 7.220 | 100.0 | 155.5 | 183.3 | 0 | 6.2 |
  | L6 | 1 | 21.500 | 16.6 | 405.6 | 472.1 | 9 | 2.1 |
  | L6 | 5 | 19.880 | 388.8 | 566.5 | 672.1 | 0 | 1.8 |

  The 20-step column of the matrix is still missing from the committed baseline
  and must be filled in by the next bench run.
- **I4. The smoke must not pass vacuously — and must be able to read back at
  all.** `webgpu-smoke.html` phase 4 pixel-compares every layer against the CPU
  renderer; on the owner's rig it printed `mean|d| NaN max|d| 0 ch>16: 0` for all
  16 layers — an empty comparison, which the old gate waved through because every
  NaN comparison is false. Two repairs, in the order the rig forced them:
  - The first attempt asked for `COPY_SRC` on the canvas
    (`GpuRenderer.init(state, { readback: true })` → `configure({ usage })`) and
    copied out of `getCurrentTexture()`. On hardware that is 32 uncaptured
    validation errors, one pair per layer:
    `[Texture "D3DImageBacking_D3DSharedImage_WebGPUSwapBufferProvider…"] usage
    (TextureBinding|RenderAttachment) doesn't include TextureUsage::CopySrc` →
    `[Invalid CommandBuffer] is invalid due to a previous error`. The usage the
    JS requests is not the usage a presented canvas gets: Chrome/D3D backs it
    with a shared image whose usage the swapchain picks, and a software adapter
    honours the request — which is why the headless rig never showed this.
  - So the readback never touches the canvas texture. With `{ readback: true }`
    the renderer draws into its own `RENDER_ATTACHMENT | COPY_SRC` texture,
    copies that into a reused `MAP_READ | COPY_DST` staging buffer
    (`readPixels()`), and blits the texture to the canvas with one
    nearest-sampled fullscreen triangle (`GpuRenderer.BLIT`), so the visible map
    and the compared pixels are the same bytes. The app's renderer keeps the
    default canvas usage and the single pass it always had.
  - And a gate that fails must say why: phase 4 reports the sample count beside
    each layer, divides by the samples actually compared, and fails with all
    three sizes (canvas, readback, CPU reference) when the comparison cannot be
    set up at all. `NaN max|d| 0` is not an interpretable line, and a rig visit
    is the only way to run this test.
- **I5. Uncaptured WebGPU errors must carry their text.** The 32 errors in the
  baseline read `webgpu uncaptured: undefined` because the message lives on
  `event.error`, not on the event. Fixed; the next log names the actual
  validation errors.
- **I6. Open defect D1 — frame-5 plate speed divergence.** The smoke fails
  `meanSpeed drift > 20%`: cpu 4.022 vs gpu 4.947 cm/yr, maxSpeed 11.449 vs
  13.026, with owner mismatch 0/10242 and |dz| max 139 m, i.e. the states agree
  but the rotations do not. Measured on the CPU reference: frame 5 is 4.022 /
  11.449 (so the CPU side of the log is exact), frame 15 is 4.940, and the same
  cells evaluated with the *unrelaxed* `omegaTarget` give 5.040 / 13.052 —
  much closer to the GPU pair than any relaxation constant reproduces
  (forcing α = 1 gives 5.130 / 12.763). The working hypothesis is therefore that
  the device's ω follows the un-relaxed least-squares fit rather than
  ω + (fit − ω)·dt/τ_ω; it is not proven, and it cannot be settled without a
  device. The smoke now prints the discriminator on every run: the worst
  per-plate |ΔR·ω| and the CPU mean/max from both the relaxed and the target ω.
  Reading the code does not explain it: the relaxation is identical in
  `js/plates.js` and `wgsl-plates.js` `reduceB` (`omega + (fit - omega) *
  min(1, dt / tau_omega)`), `tau_omega` is 0.5 in both (`params.js` →
  `wgsl-common.js` `P_TAUOMEGA`), dt is 0.1 in both (`uploadFrame` → `fDT`), both
  engines boot with dt = τ_ω so both start at the fit, and the accessors agree
  (`plateOmega` = `PLATEF[p*8+1]`, `plateOmegaTarget` = `[p*8+2]`, written by
  `setPlateOmega`, read by `velocities`, contact and diag). Ruled out: a
  `prescribedOmega`/`fixedOmega` flag left set, a missing `reduceB` dispatch, and
  `velocities` reading the target slot.
  Gate: this must be explained (and either fixed or bounded) before the parity
  claim is accepted; the ensemble bound for mean speed is 20% and the frame-5
  transient is where it is tightest. **Explained and closed (0.3-7-3):** the
  divergence was the smoke harness itself, which ran its own `Sim.raster(gpu)`
  before `GpuSim.init` + `GpuSim.raster`; `Sim.raster` runs forces on reset
  edgeType and then classifies, so the second raster fed classified edges back
  into forces and the device booted with `slab`/`collision` terms in `wEq` the
  CPU boot never had (the 0.3-7-2 INPUTS naming: `wEq` rel ~1 at frame 5 while
  `uMantle` agreed at 2.6e-5). With the harness fixed the engines agree to f32
  tracking: `meanSpeed` 4.022 = 4.022, per-plate `|ω|/|ωTarget|` 0.837 = 0.837,
  every D1 layer under the 1e-3 threshold (per-plate M/rhs/fit/ω ≤ 6.6e-7 rel,
  `uMantle` ≤ 1.3e-5, `wEq` ≤ 2.8e-4). The 20% gate was never widened.
- **I7. D1 measured, and the device now reports the numbers the theory needs.**
  The second owner-rig capture (`0.3-II-ondevice-2.txt`) carried the
  discriminator: `mean |ω|/|ω_target|` is **0.973 on the device against 0.837 on
  the CPU**, per plate 0.991/0.943, 0.966/0.691, 0.828/0.871, 1.167/0.863, with
  the speeds unchanged (cpu 4.022/11.449, gpu 4.947/13.026, cpu-from-target
  5.040/13.052). The hypothesis is now a measurement: the device's ω sits on the
  unrelaxed fit while the CPU's lags it, and `reduceB` is line-for-line
  `Plates.reduce`. Reading code has exhausted itself, so the kernel reports
  instead of being read:
  - `reduceB` leaves one witness per plate in the reduce scratch,
    `RED[RED_RELAX + p] = |ω_stored − (ω_prev + (fit − ω_prev)·α)|`, so a store
    that does not stick — or an ω something else overwrites — becomes a number.
    `zeroFrame` clears the region (its shape is `GpuSim.zeroThreads(l)`, one
    source for the kernel's branches and the dispatch count), so a frame that
    never runs K10 reports 0 rather than the previous frame or uninitialized
    scratch; `GpuSim.raster` opens with it for the same reason.
  - `diagB` folds the witnesses into `DIAG[D_RELAXERR]` and reports the dt and α
    the same frame ran with (`D_DT`, `D_ALPHA`), derived from the `frameIn` the
    K10 read.
  - `pull` mirrors slots 14..16 to `state.gpuDt` / `gpuAlpha` / `gpuRelaxErr`,
    and the smoke turns them into four named failures: the device ran a dt the JS
    did not upload; the device's α is not `min(1, dt/τ_ω)`; the stored ω is not
    the relaxed ω; and — when the aggregate still trips — the device carries the
    fit while dt, α and the stored ω all say the relaxation ran. The slot table
    exists twice (WGSL `D_*` and JS `GpuSim.DIAG`), so `tests/wgsl-struct.js`
    pins every slot, `layout.diagOut`, `reduceF`'s coverage of
    `RED_RELAX + plateCap` and `zeroFrame`'s branch shapes against the generated
    WGSL.

- **I8. The capture path: one click puts the report on the clipboard.** The
  strip rendered as a single line with `·` separators, so a capture had to be
  transcribed by hand and arrived with its structure flattened. `Perf.rows` now
  holds one entry per part (throughput, frame-gap distribution, events,
  checkpoint, kernel table) and `Perf.report()` joins them with newlines; the
  HUD renders one row each, substituting the device's own timestamp table for
  the last row on the GPU engine. `index.html`'s strip is the copy control
  (click the rows; `Clipboard.classAck`, since the rows are content rewritten in
  place at 2 Hz): it joins those rows and prefixes the engine, level, dt,
  steps/frame, view, start and seed, plus the world line (`t`, badge);
  `bench.html`'s Copy results gives the table as tab-separated text, then the
  kernel line and the raw `BENCH` lines. Both go through `js/clipboard.js`:
  `navigator.clipboard` when it accepts the write, a hidden textarea otherwise —
  the pages are opened from `file://`, where the async clipboard is routinely
  refused, so the fallback is the working path rather than legacy support
  (`tests/clipboard.js` pins both, including the refusal). `webgpu-smoke.html`'s
  Copy log goes through the same module — its log is the deliverable of a trip to
  the rig, and a silent refusal there cost a capture already.

Acceptance: per-kernel GPU ms visible at 2 Hz; baseline logs committed;
regression gate green; instrumentation < 0.1 ms/frame at L5 (measured 0.844
µs/frame of JS, `experiments/overhead-instr.js`). The 20-step bench rows
arrived in 0.3-6; the clean smoke run and defect D1 closed in 0.3-7-3 (I6).

## Phase II — The event round trip, made cheap (done on device; `dl` is what is left)

Goal: the event cadence stops being the frame hitch. The measured cause was the
transfer, not its position on the frame path, so the fix is the transfer:

- **One shared 4-byte cast buffer** for the f32-bits reinterpret. The edges
  block reinterprets twice per edge — 123k fresh `ArrayBuffer`s per transfer at
  L5, 491k at L6.
- **A light event mirror.** `GpuSim.EVENT_READS` / `EVENT_WRITES` carry only
  what `Events.cycle` reads and writes: columns, the plate table, the frame
  counters, plus `owner`/`cellPlate`/`relN`/`relT`/`edgeType`/`polarity` on the
  read side. Those cell and edge arrays are never written by the cycle (asserted
  by `tests/gpu-play.js`) and are recomputed by `raster`, `velocities`,
  `relatives`, `polarity` and `elevationZ` every frame, so shipping them back
  was cost with no effect. Checkpoints and on-demand syncs (probe, save,
  deposits, load) keep the full mirror.
- **Alive rows only**, with the alive flag past `n` zeroed so a row the device
  spawned can never be revived by a stale mirror.
- **Reused staging and pack scratch** (11 staging buffers and 4.79 MB of scratch
  per call before), with a fresh-set fallback when a probe download overlaps an
  event round trip.
- **`GpuSim.play(state, dt, n)`** is the play path's single entry point: n frames
  and their round trips in one call, the same device command sequence as n
  `step()` calls, returning the frames submitted. `step()` keeps its awaited
  contract for the parity harness and the single-Step button, and both share
  `roundTrip()`. Phase V turns `play`'s loop into one encoder.

Why the earlier "background the round trip" design was dropped, so it is not
re-proposed: awaiting a promise does not move JS off the main thread, so a
background task removes nothing from the frame gap; and a mirror that arrives
late reverts every frame the device ran in between, because the upload carries
the whole mirror — that is world corruption, not one cycle of latency.

Measured (`experiments/logs/0.3-II-roundtrip-cost.txt`, stub device, JS only,
min of 9 warm reps, before = commit c706442 under the same method):

| | L5 before | L5 after | L6 before | L6 after |
|---|---|---|---|---|
| download | 98.60 | 1.27 | 429.70 | 9.28 |
| `Events.cycle` | 2.15 | 2.09 | 7.84 | 7.92 |
| upload | 99.30 | 0.35 | 410.24 | 1.72 |
| **total** | **200.05** | **3.71** | **847.79** | **18.92** |

Result on the owner's rig (`experiments/logs/0.3-II-ondevice.txt`, nvidia/ampere,
Chrome 151, L5, 1 step/frame): frame 34.4 → 5.7 ms, p95 66.6 → 5.6 ms, max
77.9 → 11.1 ms, event round trip 114.7 → 31.7 ms, Myr/s 10.8 → 12.2 (CPU
reference: frame 11.3 ms, 9.0 Myr/s). The periodic lag is gone; the GPU now beats
the CPU engine on every distribution statistic at the same settings.

What the 31.7 ms is made of: `dl 29.4 + cyc 2.0 + up 0.3`. The JS half matches
the headless measurement, so what is left is the device — the queue drain plus
seven separate `mapAsync` round trips.

The `wait` split has now answered that (`0.3-II-ondevice-2.txt`, same rig and
settings): `events 7 (28.5 ms = dl 26.2 [wait 25.5] + cyc 2.0 + up 0.3)`, i.e.
**25.5 of the 26.2 ms download is the device finishing the work already
submitted**, and the seven `mapAsync` round trips plus the JS unpack are ~0.7 ms
on top of it. The same capture is also ~6% faster across the board than the first
one (180.0 fps, frame 5.6 ms, p95 5.6, max 5.6, 12.6 Myr/s; kernels `winners 1.66
diagC 1.46 loserRank 0.89 diagA 0.85 ledgerReduceA 0.51 subRateA 0.15 reduceA
0.14 gaps 0.12` — 5.66 ms in the top ten, against 6.74 ms), and the bench
repeated to within noise (L5 1: 8.520 / 8.800 iso ms/step, L6 5: 19.799 / 19.829).

So the round trip is no longer a transfer problem, it is a *serialization*
problem: an event cycle cannot start until the queue is empty, and the queue is
empty only after the frame it follows has fully retired. The two candidate fixes
are Phase V's (n steps in one encoder, so the drain happens once per rAF instead
of once per step) and a combined staging buffer with one `mapAsync` instead of
seven, which the `wait` number now says is worth ≤ 0.7 ms and is therefore not
worth doing first. Phase IV stays ahead of both: `diagA` + `diagC` are 2.31 ms
of that 5.66 ms top-10 total on a saturated queue and nothing on the device reads
them.

The same capture exposed a defect the light mirror introduced and the stub had
not caught: `pull` unpacked by testing the staging *cache*, which keeps buffers
from an earlier, wider download, so a light event round trip called
`getMappedRange` on a `cellF` it had never mapped. The device raised
`OperationError: getMappedRange failed`, the play promise rejected, the loop
stopped, and the run stayed stopped until play was pressed — in the app the
trigger is any full sync (probe click, Save, deposit extract, or the 20 Myr
checkpoint) followed by the next event cycle, which is why runs died at a fixed
point in time. Fixed by unpacking only what the current transfer mapped;
`tests/gpu-stub.js` now enforces the map state and `tests/gpu-play.js` runs the
full-sync-then-event sequence, so the identical stack reproduces headless.

Acceptance (owner's rig, same configs as the Phase I baseline):

- The strip's event line shows a total ≤ 10 ms at L5 and ≤ 40 ms at L6, with the
  split telling whether what is left is `dl` (queue drain) or `cyc`/`up` (JS).
  **Not met, and now attributed**: 28.5 ms at L5, of which 25.5 ms is `wait` —
  the device draining the queue — and ~3 ms is JS. No transfer-side change can
  reach 10 ms; the drain has to stop happening once per step, which is Phase V.
- p95 frame gap ≤ 2× median while playing at dt 0.1, 1 and 5 steps/frame, for a
  ≥ 200 Myr hot-start run that includes split and suture activity.
- Throughput at 1/5/20 steps ≥ baseline; the bench re-run fills in the missing
  20-step rows.
- Parity ensemble green and the release profile statistics unchanged. The
  light mirror changes *what* is transferred, not *when*, so the device command
  sequence is untouched and same-device runs must stay bit-identical.

## Phase III — The smoke must actually measure on hardware (0.5–1 d)

Goal: the one instrument that runs on hardware reports real numbers and localizes
D1 to a layer. The owner's captures 0.3-2 and 0.3-6 carry the same 32 Dawn errors
and 16 empty readbacks in both, so the pixel parity has never once run on
hardware, and D1's witnesses have settled everything except the one question
that is left. Two repairs, in the order they unblock:

- **The readback texture's usage.** The smoke's phase 4 copies pixels out of the
  renderer's own offscreen texture, and that texture needs all three of
  `RENDER_ATTACHMENT` (it is drawn into), `TEXTURE_BINDING` (the blit samples it
  onto the canvas) and `COPY_SRC` (the parity copy reads it out). The 0.3-6
  capture's error — `usage (TextureUsage::(TextureBinding|RenderAttachment))
  doesn't include TextureUsage::CopySrc` — is exactly that usage missing
  `COPY_SRC`; the identical error in 0.3-2 proves the offscreen path was in the
  build all along. What was wrong was the constant: `0x4` is `BufferUsage`'s
  `COPY_SRC`, and `TextureUsage`'s is `0x1`. The structural test defines its
  own copy of the same constant, so a value that is wrong in both passes its own
  check, and the stub device validates nothing — `tests/gpu-readback.js` now
  pins the three bits by name and the hardware capture remains the proof. The
  fix is in tree; this phase takes it to a capture.
- **The D1 cross-engine diff.** The K10 witnesses (I7) have settled the branches
  code reading left open: 0.3-6 reports `dt 0.1` (the uploaded value),
  `alpha 0.2` (= min(1, dt/τω)) and worst `|stored − relaxed| 0.00e+0`, i.e. the
  store sticks, the cap never fires and nothing overwrites the ω. What the
  witness does not cover is the fit itself — it recomputes the relaxation with
  the device's *own* `fit` and `ω_prev`, so it is self-consistent whether or not
  the device's least-squares input agrees with the CPU's, and the 0.837 vs 0.973
  ratio gap (per plate 0.275–0.304 on plates 2 and 4) is still unexplained. The
  mirror already ships everything the comparison needs: per plate `M`, `rhs`,
  `omegaTarget` (the fit) and `omega`, per cell `uMantle` and `wEq`. The diff is
  therefore CPU-side JS in the smoke, no new device code:
  - After the boot download and again after the frame-5 download, compare per
    plate: `M_dev · A0[0]` vs `M_cpu` — the device's `reduceB` stores the matrix
    and the rhs divided by `A0[0]`, so rescale before comparing — the same for
    `rhs`, `fit_dev` vs `fit_cpu`, `ω_dev` vs `ω_cpu`, as worst relative deltas
    per plate with the global worst; and the global worst over cells of
    `|uMantle_dev − uMantle_cpu|` and `|wEq_dev − wEq_cpu|`.
  - The per-plate table prints all plates (0.3-6's worst `|dR·ω|` is plate 14;
    the current table stops at the first four).
  - When the 20% mean-speed gate trips, the named failure says the layer:
    **inputs** (`uMantle`/`wEq` diverge > 1e-3 rel), **accumulation** (`M`/`rhs`
    diverge > 1e-3, inputs agree), **solve** (fit diverges > 1e-3, `M`/`rhs`
    agree — the f32 cofactor solve, including the one FMA-refinement step
    `reduceB` runs and the CPU's solve does not), or **trajectory**
    (`M`/`rhs`/fit all agree to 1e-3 but `ω` diverges — then the ω history is
    the suspect, and the boot table localizes it: a divergence already at boot
    is input/solve, not dynamics).
  - The 1e-3 threshold is 100× the f32-vs-f64 tracking the Kahan-compensated
    K10 accumulation is built for and 100× below the observed aggregate; it is
    not to be tuned until the gate passes. The parity claim and the 20% gate
    stand as they are until this capture names a layer. If the named layer turns
    out to be f32-vs-f64 accumulation, the bound for it is the 0.2-plan Phase H
    regime — 50-frame comparison with f32-calibrated tolerance plus the 1000-
    frame seed ensemble — not a widened per-frame tolerance.
- **The final render line says what was compared.** "GPU renderer drew all 16
  layers, pixel-parity checked" printed in 0.3-6 beside 16 × "readback short",
  because it keys off the readback rig existing, not on samples being compared.
  The line counts the layers that actually compared and says so.

Acceptance: one owner capture (the procedure is in the session prompt that
accompanies this plan) with (a) all 16 layers reporting finite `mean|d|` /
`max|d|` with non-zero sample counts, the comparison gate passing or failing
loudly with the layer named; (b) the boot and frame-5 D1 tables present, and
either the 20% gate passing or the named layer recorded here in this section;
(c) zero uncaptured webgpu/page errors; (d) the capture committed with its
environment line (browser, OS/CPU type, GPU type plus one vendor word) as the
baseline the later phases compare against.

Outcome (0.3-7-2, then 0.3-7-3 on the fixed harness): (a) all 16 layers
compared ~1.5 M samples each with finite `mean|d|`/`max|d|`; (b) the 20% gate
passes — the boot and frame-5 tables show every layer under the 1e-3 threshold,
so no layer was named (the 0.3-7-2 INPUTS naming was the harness's own double
raster, Phase I6); (c) zero captured errors; (d) `experiments/logs/0.3-7-3-log.txt`.
One re-confirmation of the smoke on the post-`074b5c5` build rides the closing
rig trip, since `loserRank` and `winners` changed device code after 0.3-7-3.

## Phase IV — Diagnostics off the frame path (0.5–1 d)

Goal: stop paying for numbers nobody reads every frame. The two on-device kernel
tables put the K11 passes at `diagA` 0.98 + `diagC` 1.69 = 2.67 ms of a 6.74 ms
top-10 total (first live-play capture, ~40%) and `diagA` 0.85 + `diagC` 1.46 =
2.31 ms of 5.66 ms (second, quieter capture, ~41%) — the share is stable across
saturated live-play captures even though every kernel got faster — while the
0.3-6 bench (clean queue, no round trips) puts them at 0.90 ms of a 4.24 ms
kernel sum, 21%: the absolute cost is load-dependent, from ~0.9 ms/step on the
step-isolation bench to ~2.5 ms under live play, and the acceptance below
re-measures both after the change. Nothing on the device reads the passes'
output: `diagA`/`diagC` write only the `reduce` scratch, `diagB` folds it into
the `diag` buffer, and that buffer is read only by the CPU mirror
(`meanSpeed`, masses, `oreSum`, `gaps`, `quatError`, `rigidError`, and I7's
`gpuDt`/`gpuAlpha`/`gpuRelaxErr`).

Consumers, which set the cadence:

- `js/ui.js` shows `state.meanSpeed` in the status line, refreshed on a ~150 ms
  tick — so ~6 Hz, not 60–180 Hz.
- `GpuSim.download` (full mirror) copies `diagOut`; the light event round trip
  does not touch it.
- `tests/gpu-parity.html` and `webgpu-smoke.html` compare `meanSpeed` after an
  explicit download, so a full download must be preceded by a diagnostic frame.

Design: `GpuSim.frame(state, dt, diag)`; `play` runs the three passes only when
the HUD has asked for fresh numbers since the last diagnostic frame, or when the
frame is followed by a full round trip. `step` keeps them on every frame so the
parity harness is untouched. `zeroFrame` already clears the accumulators each
frame, so an on-demand pass reports that frame's values, not an accumulation
over the skipped ones.

The bench's kernel line is printed once, at the end, from the timestamp ring,
which holds the last config's data — a mixed-level run reports a kernel table
with no label saying which level it is, and an unlabeled kernel table is a
number that cannot be cited. The bench prints the kernel line per config (or
labels it with the config it describes).

One interaction to keep honest: `diagOut` is also where I7's D1 witnesses live,
so a skipped diagnostic frame leaves the previous diagnostic frame's dt/α/residual
in the mirror. That is already the contract for `meanSpeed` ("the CPU mirror is
one event cycle old"), and the witnesses stay consistent with each other because
`diagB` derives all three in one invocation — but a capture taken mid-`play`
reports the last diagnostic frame, and the smoke must keep doing its full
download after an explicit `step`/`play` as it does now.

Acceptance:

- L5 kernel table shows `diagA`/`diagC` absent on non-diagnostic frames, and the
  step-isolation ms/step falls by their re-measured share (~0.9 ms on the clean
  bench, up to ~2.5 ms under live play — the 0.3-6 bench and the capture's kernel
  line are the before/after pair), with the per-config kernel line labeled.
  **Confirmed on the owner rig:** no `diagA`/`diagC` in any of the six
  per-config kernel lines of 0.3-7-3 or 0.3-7-4 (both carry per-config labels);
  L5 iso 1-step ms/step 8.025 (0.3-6, diag every frame) → 6.865 (0.3-7-3, diag
  on demand), a 1.16 ms re-measured share inside the predicted 0.9–2.5 band.
- `meanSpeed` in the status line and after any full download is the current
  frame's value; parity harness and smoke unchanged and green.

## Phase V — N steps in one command buffer (1 d)

Goal: the per-step fixed cost (encoder + submit + `uploadFrame` write + JS) is
paid once per rAF, not once per step. The 0.3-6 bench measures it at 0.82 ms of
an 8.025 ms step at L5 (7.204 at 5–20 steps), so the expected win is ~10% at
1 step/frame — worth taking after the diagnostics phase, not a rescue. The
1/5/20 iso spread there (8.025 / 7.184 / 7.204, 11.5%) already sits near the
±10% acceptance below.

It is also the only remaining attack on the event round trip's 25.5 ms `wait`:
that number is the device retiring the work already submitted before the cycle can
read its mirror, and one encoder for n steps means one drain per rAF instead of
one per step. Phase II's ≤ 10 ms target therefore closes here or not at all.

The smoothness arbiter for this phase is the bench's hitch fraction, and the
reason is a measurement the app's strip cannot see: the app loop keeps ticking
rAF while a round trip runs on the async path, so its p95/max gap stays ≈5.6 ms
even though the sim is stalled for the whole 28.5 ms (0.3-II-ondevice-2:
`p95 5.6 · max 5.6` in the same window that carried 7 × 28.5 ms of event time).
The bench's smooth mode re-arms its rAF only after a batch completes, so each
round trip lands as one big gap — its hitch rate is the sim-stall rate. 0.3-6:
L5 1 step 73/633 (11.5%), 5 steps 66/141 (47%); the cadence is
`eventCadence / dt` = 10 frames at dt 0.1, one ~28.5 ms round trip per 10. The
Phase II `p95 ≤ 2× median` acceptance was measured with the blind statistic and
is superseded here; the app strip keeps its distribution line for frame cost,
not for cadence.

- `GpuSim.play` precomputes n `frameIn` blocks on the CPU before encoding: the
  mantle bookkeeping (`Tm`, precession, plume spawns, wave phase) is sequential
  and cheap, and keeping it on the CPU is the design's split. `frameIn` becomes
  n×74 floats (the buffer grows with the max n, 20); one encoder carries n× the
  dispatch list of `frame()`, one submit.
- The frameIn bind group names its per-block size (`offset: 0, size: stride`).
  A dynamic-offset binding whose range is the whole buffer is illegal the moment
  the offset is nonzero: 512 B into a 20-block 10240 B buffer is out of bounds,
  the encoder is invalid, and `onSubmittedWorkDone` returns in a fraction of a
  millisecond because the GPU ran nothing. Offset 0 happens to fit, which is why
  1-step play kept working while 5-step iso and the smoke's `play(5)` did not.
- Same dispatch order and same input values as n separate `frame()` calls, so
  results must stay bit-identical on the same device: the parity harness and the
  frame-2 clone test are the checks (the clone test specifically catches "same
  numbers, wrong decision path" drift).

Acceptance:

- GPU ms/step from the step-isolation bench flat across 1/5/20 within 10%.
  **Re-expressed on the 0.3-7-4 measurement (recorded plan change):** the gate
  becomes the absolute 1-step premium, not the relative spread. Measured iso
  L5 3.895/3.062/2.800 (1-step premium 1.10 ms) and L6 8.970/8.222/8.259
  (premium 0.71 ms). The per-rAF fixed cost the gate was sized against is
  unchanged (~0.82 ms in 0.3-6, ~1.0 ms now — encoder + submit + `uploadFrame`
  + JS, unamortizable by construction at 1 step/frame); it is the marginal step
  that fell from ~7.2 ms to ~2.8 ms, so the old ±10% relative reading is
  denominator-broken, not missed. L6 reads 8.6% under the old rule either way.
- Bench smooth-mode hitch fraction (the sim-stall arbiter above): L5 1 step
  ≤ 2% of frames, 5 steps ≤ 10% — one drain per rAF instead of one per step is
  what makes that reachable; the app strip's p95/max is not a gate for cadence.
  **Met on 0.3-7-4:** L5 0/420 (0%) and 1/231 (0.4%), L6 4/333 (1.2%), 0/97,
  0/26 — down from 0.3-6's 11.5%/47%.
- Parity harness: same-device bit-identical; ensemble green. Met on the owner
  rig 2026-09-16 on the final build — ensemble `gpu-parity-2026-09-16-06-26.log`,
  batch + determinism + ensemble re-run `gpu-parity-2026-09-16-07-13.log` (chrome 151,
  gpu hardware nvidia; 3 × 1000 frames inside the bounds, all eight frame counts
  bit-identical, two-run determinism bit-identical). The 07:13 pass ran under a
  heavily loaded GPU and reproduced the idle seed numbers bit for bit — the verdicts
  are load-independent by construction (nothing in the step path reads wall clock),
  only wall ms are not. The run reached hardware at all only after the rig itself
  was fixed (init asked for the software adapter first on browsers without the
  SwiftShader flag; findings, “the parity rig on Windows”). Phase V closed.

## Phase VI — `loserRank` without the O(losers × n) scan (0.5–1 d)

Goal: turn the only superlinear kernel into a bounded one. Measured share at L5
on a saturated queue: 1.09 ms, ~16% of the top-10 kernel time (0.29 ms and ~4%
when the queue was still stalling on the event round trip); the heavy-overlap
epoch is worse still.

- Losers are already binned per winner (`loserCount` + `scanC` offsets).
  Rewrite as one workgroup per winner: load the winner's L_w bin entries into
  workgroup memory, compute each entry's rank by index (L_w²/2 local
  comparisons, trivially parallel in the workgroup), write `loseList`.
  Worst case is one giant winner (L_w ~ colCap during a mega-collision):
  chunked workgroup memory handles it; typical L_w is single digits.
- The rank definition (index order among a winner's losers) is unchanged, so
  the output list is identical — a bit-identical refactor, proven by the parity
  harness.

Acceptance:

- Contact-block GPU ms (I1 query sets) reduced ≥ 5× in a heavy-overlap rig — a
  new `experiments/` config that maximizes per-frame overlaps (e.g.
  `plateCount 4`, low `vSuture`, hot start) with the overlap count logged from
  the frame counters to show the load was real. Re-expressed after the two
  rewrites (barrier-free per-lane ranking, then the grid-stride dispatch cap):
  no in-tree pre exists to capture against, so the final-build heavy-overlap
  capture is judged against the same rig's L5 bench-world line instead of
  claiming an unreproducible pre/post ratio. The idle 2026-09-16 08:30 capture
  logs 9.78 mean / 36 max overlaps per frame, a 0.311 ms contact block and
  `loserRank` 0.262 ms; that is inside the idle 0.21–0.34 ms L5 bench-world
  interval. The collision-dense load causes no measured loser-rank regression;
  Phase VI is closed on that final-build measurement.
- Parity: same-device bit-identical; ensemble green — met 2026-09-16
  (`gpu-parity-2026-09-16-07-13.log`, batch/determinism/ensemble all PASS).

## Phase VII — Polish and L6 (closed on the instrumented real-drag capture)

- Merge the render pass into the play encoder: one submit per rAF for sim + draw
  instead of two (acquire the canvas texture, append the render pass after the
  compute passes in `play`). A round trip must `await`, and `getCurrentTexture`
  after a yield presents black (L6 flash once per event, L7 blank until pause
  or a view-only draw). So: if an event is due at the start of `play`, submit
  the draw on its own encoder first, then await; the tail is consumed once and
  later segments of that call do not acquire the canvas again. Non-event rAFs
  stay one submit.
- The step gate is the view, never the pointer, on both engines: a frame defers
  its step while the view is moving and for `VIEW_HOLD_FRAMES` (3) frames after
  it stops, so a held-still button runs the sim and a resting pointer resumes it
  without waiting for the release. The CPU's reason is its own re-sample
  (~18 ms at L5); the GPU's is that a batch containing the event round trip
  holds the device queue for the readback (~25.5 ms of the 28.5 ms measured on
  device) and then the main thread for the unpack and the cycle, and submits its
  compute ahead of the drag's draws — one stutter per cadence, i.e. every
  `eventCadence / dt` frames. The hold is what makes the gate hold: a drag whose
  pointermove stream is thinner than the frame rate leaves empty frames, and one
  empty frame is enough for a batch to start mid-drag. `GpuSim.play(state, dt, n,
  hold)` polls the page's predicate at its frame boundary — the only point in a
  batch where the event loop runs — so a batch a drag catches mid-flight stops
  there and reports the frames it submitted; the frames it did not run are the
  next batch's, so no cycle is skipped and no sim time passes unaccounted.
- The copied perf report records a non-dead-zone pan only after it happened:
  drag count, pan moves and pixels, plus the number of gate closures, deferred
  rAFs and re-openings while playing. The closing hardware capture must include
  a nonzero `view gate:` line after a real L6 or L7 drag; it proves the gate
  actually deferred work and resumed it, while the operator records that the
  visible map stayed drawn rather than flashing black.
- UI: level select over L5 / L6 / L7 on both engines, rebuilding the world like
  Reset world does (the probe hint still steers L6 and L7 to the GPU engine per
  the "CPU JS first (L5), then WebGPU (L6–L7)" policy), shipped with URL
  pre-fill, a provenance-bearing copy header and load-through-the-blob's-level;
  see archive/0.3-4-report.txt. Open: an event-cadence select (1 / 5 / 10 Myr)
  as the user-facing fast-forward escape hatch; default stays 1 Myr so the
  canonical run is unchanged.
- The perf strip reserves one line track per part of the readout (Perf.SLOTS)
  and rewrites their text in place, so variable-size info cannot move the page
  (the owner's second 0.3-3 task); measured before/after in
  experiments/logs/0.3-III-hud-reflow.txt. The clock and the column inspector
  got the same reservation.
- Bench matrix extended to L6 (both modes, 1/5/20 steps); the 20-step rows are
  reachable now — the steps bound used to drop the bench's own default 20 — and
  dropped configs are named (`BENCH ignored:`). L6 baseline committed.

Acceptance: L6 playable at 1–5 steps/frame with p95 ≤ 2× median on the
calibration host's hardware; one real L6 or L7 drag while playing records a
nonzero `view gate:` line with a closure, deferred rAFs and a re-opening, and
the visible map remains drawn throughout. **Met 2026-09-16 09:41:** the L6 GPU
capture records 2 drags, 554 moves / 1498 px, 5 gate closures, 653 deferred
rAFs and 5 re-openings; the operator confirmed no black or blank frame. The
existing L6 bench matrix supplies the p95 gate. SwiftShader numbers are relative
only (existing convention). All earlier gates remain green.

**Plan closed 2026-09-16 on the final build.** Release gates: full profile PASS
(29 tests, 537 s, `experiments/logs/full-test-2026-09-16-10-26.log` — longrun
1500 Myr ends 19.1% continental with 74 splits / 81 merges, and the dt 0.01
300-Myr cross-check matches at the statistics level); headless GPU parity
ensemble PASS on hardware (`gpu-parity-2026-09-16-10-25.log`, chrome 150,
gpu hardware nvidia, seeds 7/8/9 all inside the predeclared bounds); the
hardware smoke re-confirmation PASS with 16/16 layers pixel-compared and zero
captured errors (`webgpu-smoke-2026-09-16-07-13.log`).

## Risks and how the plan handles them

- **The light mirror ships too little.** The claim is narrow and asserted:
  `Events.cycle` writes only the columns, the plate table and the counters, and
  the frame kernels recompute every cell and edge array it reads before anything
  reads them. `tests/gpu-play.js` fails if a cell or edge buffer survives an
  event round trip written, and the parity ensemble plus the release profile are
  the arbiters on device.
- **Query-set portability** (missing on some SwiftShader builds): feature
  detect, wall-clock fallback, and the headless rig documents the flags.
- **Event staleness changing long-run statistics:** the cadence is never skipped
  (`play` runs the same round trips in the same order as `step`, and a batch the
  drag gate stops does not advance the frames it did not submit, so no sim time
  passes without its cycle), `Events.cycle` is span-aware and gets the span of
  the due date, and the gates are the parity ensemble plus the release profile;
  any statistics drift is a plan failure, not a tunable.
- **N-step encoder breaking parity:** bit-identity is an acceptance criterion,
  checked by the frame-2 clone test and the 1000-frame ensemble before the phase
  is closed.
- **Bench on the wrong device:** every reported number carries its device string
  (one short line at boot: date to the minute, browser, OS/CPU type, GPU type plus
  one vendor word — `js/env.js`; no UA string, no model name); SwiftShader is labeled
  relative-only, matching the existing BENCH convention in the sibling project.
- **Defect D1 hiding behind a passing ensemble:** the ensemble samples every 10
  frames, and the frame-5 transient is exactly where the mean-speed bound is
  tightest. The smoke's K10 witness line is the standing check on the relaxation
  arithmetic itself, and the cross-engine diff (Phase III) must name the layer a
  divergence lives in before the 20% gate may be treated as met. A closure on
  "f32 vs f64" without that comparison is a hypothesis, not a result: the
  witness recomputes the device's own formula and is self-consistent either way.

## Milestones

| Phase | Deliverable | Gate | Status |
|---|---|---|---|
| I | query sets, p95/max strip, bench (headless + in-page), committed baseline, smoke that cannot pass vacuously or fail inexplicably (I4 offscreen readback, I7 device-reported witnesses), one-click capture (I8) | regression gate + baseline logs | done; the 20-step bench rows were a code defect (the steps bound dropped the bench's own default), fixed and pinned, and the rows arrived in the 0.3-6 capture (iso L5 8.025/7.184/7.204, L6 21.480/19.537/19.924). The clean smoke arrived in 0.3-7-3 and closed defect D1 (Phase I6) |
| II | light event mirror, shared cast buffer, alive-rows-only transfer, reused staging, `GpuSim.play`, three-phase event timing | event total ≤ 10 ms (L5) on the owner's rig, p95 ≤ 2× median, parity green | done on device and re-confirmed: frame 5.6 ms, p95 5.6, max 5.6, event 28.5 ms = dl 26.2 **[wait 25.5]** + cyc 2.0 + up 0.3. The 10 ms target is closed as a Phase II goal — the remaining cost is the queue drain, which is Phase V's. The `p95 ≤ 2× median` half was measured with the app strip, which is blind to the async stall (Phase V); the stall it does not see is 0.3-6's 73/633 hitched frames at L5 1 step |
| III | smoke on hardware: offscreen readback with the full usage (pinned in tests/gpu-readback.js), the D1 cross-engine diff (boot + frame 5: M/rhs/fit/ω per plate, uMantle/wEq per cell), the final render line counts samples compared | one owner capture: 16 layers with finite mean\|d\| and non-zero samples, the D1 table with the named layer or the 20% gate passing, zero uncaptured errors | done on hardware (0.3-7-3): PASS — 16/16 layers pixel-compared (~1.5 M samples each), boot + frame-5 D1 tables all under the 1e-3 threshold so no layer named, meanSpeed 4.022 = 4.022, zero captured errors. D1 closed as the smoke harness's double raster (Phase I6), not an engine divergence; the 20% gate was never widened. The riding re-confirmation arrived 2026-09-16 07:13 on the final build (`webgpu-smoke-2026-09-16-07-13.log`): PASS, 16/16 layers pixel-compared, boot + frame-5 D1 tables all ≤1.5e-6, zero captured errors, non-empty `[ts]` — the whole timestamp ring reads end-to-end on hardware |
| IV | `frame(state, dt, diag)`: the K11 passes only when the HUD or a full download asks | diagA/diagC off non-diagnostic frames, ms/step back by the re-measured share (~0.9 ms clean bench / ~2.5 ms live play), per-config kernel line labeled, parity green | in tree: wantDiag at the 150 ms HUD tick, wants coalesce, full download always folds diagFrame, light event round trips run no K11, bench iso is the diag-off path with tsReset per config; pinned by tests/gpu-play.js §7 and tests/gui.js. Owner bench confirmed (0.3-7-3, 0.3-7-4): no diagA/diagC in any per-config kernel line; L5 iso 1-step 8.025 → 6.865 ms on the diag on/off pair = the re-measured 1.16 ms share (predicted 0.9–2.5) |
| V | one encoder per rAF for n steps | ms/step flat across 1/5/20 (±10%), bench hitch fraction L5 1 step ≤ 2% / 5 steps ≤ 10%, bit-identical | in tree: FIN_MAX 20 padded 74-float blocks, dynamic frameIn offset with bind-group size = stride (Dawn rejects a nonzero offset on an unspecified whole-buffer range; 0.3-7's 0.586 ms/step L5×5 was an invalid encoder), per-frame t/frame adds (never run·dt), batch() timestamps frame 0, play segments at round-trip boundaries, drag gate polls per segment and defers never skips; sandbox-pinned (gpu-play §8 block/offset/size/FIN_MAX/tail-copy; `--batch` rig over eight frame counts). Bench gates met on the final build (0.3-7-4): iso L5 3.895/3.062/2.800, L6 8.970/8.222/8.259; hitches L5 0/420 and 1/231 vs the ≤2%/≤10% gates, L6 4/333, 0/97, 0/26 (0.3-6 was 11.5%/47%). Flatness gate re-expressed as the absolute 1-step premium (1.10 ms L5, 0.71 ms L6 — the unchanged per-rAF fixed cost; the ±10% relative read is denominator-broken at a 2.8 ms marginal step, recorded in the Phase V acceptance). owner-rig ensemble + batch + determinism green 2026-09-16 on hardware (`gpu-parity-2026-09-16-06-26.log`, `gpu-parity-2026-09-16-07-13.log`, chrome 151 win, nvidia) — closed |
| VI | per-winner `loserRank` | contact ms ≥ 5× better under heavy overlaps, bit-identical | in tree: loserScatter bins via atomic cursor, loserRank is one workgroup per winner, barrier-free per-lane sweep of each entry against the whole bin (O(Lw²) per workgroup, bin length unbounded; workgroup barriers are illegal here - aliveN and the bin length are memory loads, so their guards and loops are non-uniform control flow to the validator); JS port of the algorithm pinned in gpu-play §9 including a 400-loser mega-bin; experiments/heavy-overlap.html (+ driver) logs the overlap load and contact-block ms. Owner gate re-expressed: the pre-refactor kernel left the tree with the two rewrites, so no in-tree pre exists to capture against — the capture is the heavy-overlap rig on the final build logging the overlap load and the loserRank/contact line, judged against the bench-world line (0.3-7-4: loserRank 0.21–0.34 L5, 0.90–0.97 L6). Idle re-take 2026-09-16 08:30 (`0.3-7-6-log-2026-09-16-08.txt`): real load (mean 9.78 overlaps/frame, max 36), 0.311 ms contact block and 0.262 ms loserRank, inside the L5 bench-world interval — closed |
| VII | single submit per rAF, level + cadence UI, L6 baseline | L6 frame-gap p95 ≤ 2× median (frame cost; cadence is the bench hitch fraction per Phase V), all gates green | in tree: the draw (GpuRenderer.appendTo) is appended to the first play segment encoder so sim plus visible frame is one submit; if an event is already due, the draw is submitted on its own encoder before the round-trip await (getCurrentTexture after a yield presents black); later segments of that call do not acquire the canvas; standalone draw remains the paused/view-only path; cadence select 1/5/10 Myr applies live and ships in the copy header; bench smooth now uses the merged path; pinned by tests/gui.js (render-tail assertions, cadence options/query/live change) and tests/gpu-play.js §10. Level select, reserved strip and drag gate were already in tree. L6 bench matrix committed (0.3-7-4): smooth p95/med 27.8/16.7, 66.7/44.5, 222.2/194.4 at 1/5/20 steps, all ≤ 2×. Final owner capture 2026-09-16 09:41 (`0.3-7-7-drag-2026-09-16-09.txt`): L6 GPU play records 2 drags / 554 moves / 1498 px, 5 gate closures / 653 deferred rAFs / 5 re-openings, with the operator confirming a continuously drawn map — closed |
