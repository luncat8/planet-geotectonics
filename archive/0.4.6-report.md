**What I got wrong.** I told you "South America's existence window ends at 143.8 Ma, Australia at 94, the Pacific at 84 — a 250 Ma reconstruction has no model value for them." False. Plate 201's rotation lines are `-250, -100, -75, 0, 0.99, … 143.8, 1100.0` — my own ingest's `--tmax=540` was dropping the 1100 Ma sample, and I read the truncated table as the model. With the full range there are **zero** single-sample plates (I'd claimed 136), and 257 of 258 plates reach past 540 Ma. The table now spans 0–1100 Ma.

What the long stages actually mean is better than what I claimed: the angles are **identical at both ends** — Australia 33.82° relative to Antarctica at both 94 and 1100 Ma, South America 57.49° relative to Africa at both 143.8 and 1100, India 56.76° relative to Madagascar at both 206 and 1100. That's the model saying "Australia *was part of* Antarctica", so reconstructing across it is exact, not an approximation. Verified: the relative rotations of AUS and ANT over that interval differ by 0.0000°.

**Two bugs the truncation had been hiding.** With stages over 1000 Myr, rotations can exceed 180°:
- `at()` slerped the two samples. A slerp takes the *shortest* arc, so plate 222's 186.5° stage (79.1→1100 Ma) was interpolated the wrong way round — mid-stage off by exactly 180°. It now exponentiates the fractional stage in the plate's frame, exact at both ends for any angle.
- `pole()` used `atan2(s, |w|)`, folding any stage over 180° onto its reverse. Now signed `w`.

**The polygons.** `PALEOMAP_PlatePolygons.gpml` (5.3 MB, MD5 `d782fab5…`) vendored to `data/earth/`; I dropped the duplicate `.rot`, the modern political borders, and the raster descriptor. `tools/earth/gpml_plates.js` reconstructs the 503 rings with `js/rotations.js` onto `bake_earth.py`'s lattice — 223 plates at 0 Ma covering 93.7% of cells, 133 at 100 Ma (39%), 85 at 250 Ma (23.8%, continental only, since the ocean floor of 250 Ma is subducted and isn't in the file). Two bugs of my own in it: the emitted table was sorted by cell count while the grid held id-ordered indices (consumers lined up the wrong plates), and overlapping rings resolved by archive order — now by smaller area.

**It produces real Pangaea.** Centroids at 250 Ma: Siberia 61°N 52°E, Africa 49°S 1°W, North America 21°N 21°W, South America 21°S 21°W, Antarctica 27°S 86°E, Australia 28°S 134°E, India on the equator at 72°E. The sutures hold: great-circle paths NAM→AFR, SAM→AFR and IND→ANT are **100% continental**, AUS→ANT 97%, against a Siberia→Australia control at 62% that must cross the Tethys. Identity at 0 Ma is right cell-for-cell (Africa→701, NAM→101, AUS→801, ANT→802, IND→501, PAC→901). Against the modern pack it agrees 46.6%, and every large disagreement is the pack's nearest-seed Voronoi handing open ocean to a sliver plate (`pa` vs `jf`/`mq`/`ps`) — the reference is the weak link, which is the argument for replacing it.

**Verified by running:** `node tests/rotations.js` → PASS (883 stages consistent to 3.4e-6°); `node tests/gpml-plates.js` → PASS; `node --expose-gc tests/run-all.js` → **PASS all tests (short profile, 32 tests in 133 s)**, executing `Rotations.at`/`pole`/`relative` and `gpml_plates.js`'s parse→reconstruct→rasterize path. Committed and pushed as `571574d`.

**Next:** wire `gpml_plates.js` output into `bake_earth.py` as a `--plate-ids` mode so epoch packs carry real plate ids *and* real poles (which also stops the zero-pole guard from firing), then 0.4.6b's `Earth.reconstruct`. One caveat worth your call: the polygons and the PaleoDEM are not the same vintage — at 250 Ma they overlap on 10,871 of the pack's 24,687 continental cells (IoU 0.372). That's a measured ceiling on the reconstruction gate, not a bug, but if the PaleoAtlas has a `Coastlines.gpml` or craton polygons in that archive, they'd tighten it.