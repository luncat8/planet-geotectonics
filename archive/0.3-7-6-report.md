# 0.3-7-6 capture report

## Evidence

- `experiments/logs/0.3-7-6-log-2026-09-16-08.txt` is the idle owner-rig capture: Chrome 151, Windows x86_64, hardware NVIDIA.
- `experiments/logs/gpu-parity-2026-09-16-08-49.log` is the independent headless ensemble capture: Chrome 150, Windows x86_64, hardware NVIDIA.

## Phase VI — heavy-overlap timing closed

The prescribed L5 heavy-overlap rig used hot start, four plates, `vSuture=300`, seed 11, 60 load frames and 200 timestamped isolation frames. It recorded 9.78 mean overlaps per frame and 36 maximum, so the collision-dense workload is real.

| Measurement | Idle result |
|---|---:|
| Contact block | 0.311 ms/frame |
| `loserRank` | 0.2621 ms/frame |
| `loserScatter` | 0.0000 ms/frame at the timestamp resolution |
| Full isolation wall time | 2.52 ms/frame |

`loserRank` is inside the idle L5 bench-world interval of 0.21–0.34 ms from the 0.3-7-4 baseline. The earlier 0.754 ms loaded capture was not comparable; this idle repeat is. Since the pre-refactor kernel is no longer in the tree, this is a final-build non-regression measurement, not an unsupported claim of a recreated 5× ratio.

## Phase V correctness gate repeated

The owner-browser capture records all of the following as PASS on Chrome 151:

- batch encoders are bit-identical to one-frame encoders for 1, 7, 11, 12, 20, 21, 25 and 40 frames;
- two 8-frame runs from the same upload are bit-identical;
- the 1,000-frame ensemble for seeds 7, 8 and 9 remains inside the predeclared plate, column, mean-speed, max-speed and continent bounds.

The separate headless capture repeats the 1,000-frame ensemble on Chrome 150 and reports the same per-seed end statistics and PASS verdict. Browser/driver version changes affected wall time only; no state result changed.

The one-frame exact parity run still reports the known frame-0 drainage `low` tie difference and its two downstream sediment cells. It is the documented divergence locator, not a release gate: batch identity, determinism, the smoke and both ensemble captures remain green.

## Phase VII — final capture is now auditable

The copied performance strip previously carried no proof that its operator had actually panned while playing. It now emits a `view gate:` line after a real pan with drag/move/pixel counts and the gate's closures, deferred rAFs and re-openings. This is pinned in `tests/gui.js` for both engines.

The remaining hardware step is therefore specific:

1. Open `index.html?level=6&engine=gpu&steps=5` (L7 is also valid).
2. Let the GPU world boot, press **Play**, drag the map through several visible moves, then leave it at rest long enough for stepping to resume.
3. Confirm visually that the map remains drawn throughout; click the performance strip and save the copied text.
4. The saved report must include a nonzero `view gate:` line with at least one closure, deferred rAF and re-opening.

That capture is the only remaining Phase VII owner-rig gate.
