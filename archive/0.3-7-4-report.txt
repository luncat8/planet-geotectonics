## Status confirmed by this capture

The `074b5c5` build (grid-stride loserRank + winnerA/winnerB split + lost-device handling) plus this bench close out most of the remaining owner gates. Compared with the same-day 16:42 run and the Phase I baseline:

| | iso L5 1/5/20 | iso L6 1/5/20 | hitches L5 1 step | Myr/s L5 ×20 |
|---|---|---|---|---|
| Phase I baseline | 8.900 / 7.220 / — | 21.500 / 19.880 / — | 33 | — |
| 16:42 (pre-fix) | 6.865 / 6.034 / 6.179 | 21.355 / 19.845 / 20.189 | 115/618 | 16.4 |
| **17:48 (this capture)** | **3.895 / 3.062 / 2.800** | **8.970 / 8.222 / 8.259** | **0/420** | **27.4** |

- **Phase IV — closed on hardware.** No `diagA`/`diagC` in any of the six labeled per-config kernel lines; the on/off bench pair (0.3-6 → 0.3-7-3) re-measures the diag share at 1.16 ms, inside the predicted 0.9–2.5 band.
- **Phase V — hitch gates met decisively:** L5 0/420 (≤2%) and 1/231 (≤10%), L6 4/333, 0/97, 0/26, vs 0.3-6's 11.5%/47%. The ±10% iso-flatness gate reads 39% at L5, but the absolute 1-step premium is 1.10 ms (0.71 ms at L6, and L6 is 8.6% anyway) — the same ~0.82–1 ms per-rAF fixed cost the phase was sized against; it's the marginal step that fell 7.2 → 2.8 ms, so the relative gate is denominator-broken. I recorded that as an explicit plan change (absolute premium, not a silent re-read), matching the plan's convention.
- **Phase VII — L6 matrix committed:** p95/med = 1.66 / 1.50 / 1.14 at 1/5/20 steps, all ≤ 2×.
- One caveat on the L5 1-step Myr/s (8.4 vs 12.3 at 16:42): that's smooth-mode completion latency (median gap 3 rAF slots vs 1), not engine speed — iso is 43% faster and hitches went to zero, which Phase V names as the arbiter.

Also worth stating plainly: **D1 is closed.** The 0.3-7-3 smoke (16:40) is a full PASS — 16/16 layers pixel-compared, boot + frame-5 D1 tables all under the 1e-3 threshold, `meanSpeed` 4.022 = 4.022 — and the divergence was the smoke harness's own double raster, not an engine defect; the 20% gate was never touched. The plan file hadn't recorded that closure yet; it does now.

## What I changed

- `experiments/logs/0.3-7-4-log-2026-09-15-17.txt` — your capture, verbatim, as the new committed baseline.
- `0.3-plan.md` — recorded the closures above in the I6 gate, Phase III/IV/V/VI acceptances, and the milestone table; fixed the stale Phase VII heading. No code touched: every phase is in tree, and the short profile (24/24) plus the GPU test subset stay green.

## What's left — one closing rig trip (all on your machine; the sandbox has no GPU)

1. `webgpu-smoke.html` → wait for the result line → **Copy log** → paste. D1 re-confirmation on the post-`074b5c5` build (loserRank/winners changed device code after the 16:40 PASS).
2. `run_gpu_parity.py` — the headless parity ensemble: `--batch` zero-tolerance plus `1000 --ensemble` on the final build (Phase V's last owed gates).
3. `run_full_test.py` — the full profile incl. longrun on the final build.
4. `experiments/heavy-overlap.html` → copy the overlap load + contact-block line (Phase VI; the pre/post framing is gone since the old kernel left the tree — the plan now judges it against the bench-world line).
5. One real drag while playing at L6 or L7, click the perf strip, paste (Phase VII's last open gate).